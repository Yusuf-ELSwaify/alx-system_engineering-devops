#!/usr/bin/env bash
# print the time hours and minutes using while loop

minute=0
hour=0

while [ $hour -le 12 ]
do
  if [ $minute -eq 60 ]
  then
    (( minute = 0 ))
  fi

  if [ $minute -eq 0 ]
  then
    echo "Hour:" $hour
    (( hour++ ))
  else
    echo $minute
  fi

  (( minute++ ))
done
